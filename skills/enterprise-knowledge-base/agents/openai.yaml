interface:
  display_name: "企业知识库"
  short_description: "一个入口初始化、管理并确认式发布知识"
  default_prompt: "Use $enterprise-knowledge-base to initialize or operate the current Codex project as a local knowledge Vault, including attachment collection, audited questions, media transcription, and explicitly confirmed Feishu publication."

policy:
  allow_implicit_invocation: true
