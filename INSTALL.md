# 企业知识库安装指引

安装代理必须先读取并遵循 [INSTALL_REQUIREMENTS.md](INSTALL_REQUIREMENTS.md)。本页只保留面向用户的配置步骤。

## 安装插件

1. 在 Codex 的插件安装界面选择本插件安装包。
2. 安装后只应看到一个 Skill：`企业知识库`。
3. 回复 Codex 显示的本地自动配置确认和使用角色。
4. Codex 自动完成其余本地步骤；不要自行下载文件、创建目录、输入路径或执行命令。

Windows 与 macOS 使用同一流程。Codex 默认在系统“文档”目录创建 `知识库`，并将它同时配置为 Codex 项目根目录和 Obsidian Vault 根目录。Codex 自动安装或复用 Obsidian、创建目录结构、打开 Vault、安装或复用 Claudian，并配置本机 Codex CLI。若 Codex 桌面版无法自动登记项目，用户最多只需执行一次“在 Codex 中打开知识库文件夹”。

默认 `知识库` 目录若已是有效 Vault 则原样复用；若为空则自动初始化；若是旧版三 Skill Vault，则自动备份旧规则并把旧 Skill 移入 `.kb` 的可恢复备份区；若包含其他无关文件则停止并请用户决定，绝不覆盖或自动换目录。安装后 Skill 通过项目规则和同根绑定限定在该项目内，不能用环境变量或显式路径跳转到其他项目。

Claudian 是新机首次配置能力的一环，不是每次运行都要安装的依赖。向导先只读检查：已配置且支持 Codex 的 `YishenTu/claudian` 会原样复用，不下载、不覆盖；仅在缺失或版本不兼容且用户已确认本地自动配置时，才从固定校验版本安装并启用 Obsidian 社区插件 `Claudian`（插件 ID：`realclaudian`）。找不到 Codex CLI、资产哈希不匹配或插件回读不一致时安装失败关闭。

## 飞书管理员首次配置

1. 选择 `我是飞书管理员，首次为公司部署`。
2. 选择或创建企业自建应用，并在 Codex 内置浏览器完成管理员操作与本人 OAuth。
3. 选择唯一的企业知识空间。
4. 查看 `全公司内部员工 + 成员 + 查询、收录并发布 + 外部分享关闭` 的权限预览。
5. 只有确认预览无误后，输入向导给出的成员更新确认语句。
6. 等待写后回读通过，再导出无密钥公司配置文件。

应用安装和 OAuth 不等于获得知识空间权限。成员变更是独立远端写入；未确认时为零写入。普通员工只取得空间成员角色，不取得空间管理员角色。

## 普通员工加入

1. 选择 `我是普通员工，加入公司已有知识库`。
2. 选择管理员提供的无密钥公司配置文件。
3. 在 Codex 内置浏览器使用员工本人飞书账号完成 OAuth。
4. 等待租户、空间、映射、成员权限和全员功能策略验证。
5. 验证通过后，向导执行第一次只读企业知识同步。

完成后，员工可在 Codex 中直接查询、收录并发布同一套企业知识。每个新的 Codex 任务第一次查询全部知识时只做一次前台增量同步；同一任务后续查询复用本地镜像。输入 `同步公司知识` 可主动同步。系统没有后台轮询或常驻服务。

## 配置后的边界

- 管理员与员工都能查询、收录和发布经过审核的企业知识。
- 管理员与员工的个人知识一律纯本地，不能发布、上传原件或进入员工镜像。
- 本地收录不会自动发布；企业发布仍需独立预览和 `确认批量发布`。
- 员工镜像只来自已验证飞书企业知识空间中的已发布内容，不复制管理员 Vault。
- 外部分享保持关闭；映射、成员、远端节点或本地镜像出现矛盾时失败关闭。
