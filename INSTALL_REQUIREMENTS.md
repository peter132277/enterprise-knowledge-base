# Codex 桌面版安装要求

本文档供执行安装的 Codex 使用。用户只需复制 README 中的一句话；安装代理负责完成以下检查和引导。

## 安装范围

1. 从本仓库安装最新稳定版 `enterprise-knowledge-base` 插件。
2. 只安装仓库中经过校验的单插件分发内容；安装后 Skill 界面只显示一个 `企业知识库`，不得把内部能力拆成多个独立 Skill。
3. 安装前校验插件清单、版本、单 Skill 结构和分发内容；不得安装测试目录、缓存、数据库、后台服务或开发期临时文件。
4. Windows 和 macOS 使用当前系统支持的本地插件安装方式，不要求用户执行 CLI 命令。

## 依赖与本地目录

1. 先运行只读安装计划；不得要求用户下载文件、创建目录、输入路径或执行命令。
2. 默认解析系统“文档”目录并使用其 `知识库` 子目录；目录名同时是 Codex 项目名。
3. 用户确认一次本地自动配置后，由 Codex 检测并安装或复用 Obsidian、创建 Vault、写入项目专用同根绑定、打开 Obsidian、安装或复用 Claudian并配置 Codex CLI；不得把运行时访问重定向到其他项目。
4. 默认目录不存在或为空时自动创建；有效 Vault 原样复用；精确匹配旧版三 Skill 结构时先备份旧规则并把旧 Skill 移入 `.kb/legacy-skill-backup/v0.4.1`；其他非空目录失败关闭且不得覆盖或静默改名。
5. 如 Codex 桌面版不能程序化登记项目，只要求一次 `在 Codex 中打开“知识库”文件夹`，随后从本地状态继续。
6. Claudian 安装与 Codex 路径配置只属于新机首次配置子流程。先只读识别当前 Vault 中已安装、已启用且支持 Codex 的官方 `YishenTu/claudian`（插件 ID：`realclaudian`）；若已配置完成则原样复用。仅当缺失或不兼容且本地自动配置已获确认时，才安装固定校验版本并校验三个发布资产的 SHA-256。
7. Claudian 或 Codex CLI 缺失、下载资产不匹配、配置回读失败时不得报告完成；不得静默改装成名称相似的其他 Obsidian 插件。
8. 不新增数据库、后台轮询、常驻服务、定时任务、专用 MCP、自动模型下载或第二套认证。

## 安装后的连续配置

1. 安装成功后不得要求重启 Codex；只有 Codex 项目登记无法自动完成时才允许一次打开文件夹操作。
2. 立即读取新安装的 `enterprise-knowledge-base/SKILL.md`，在当前任务直接运行安装计划和本地自动配置；插件列表稍后刷新不得阻断配置。
3. 首次只向用户显示以下三个入口：
   - `我是飞书管理员，首次为公司部署`
   - `我是普通员工，加入公司已有知识库`
   - `暂时只使用本地知识库`
4. 后续每一步只显示当前需要的可读选项，不要求非技术用户复制命令、内部 ID、Token 或密钥。

## 权限与远端安全

1. 安装插件、导入公司配置和完成 OAuth 都不等于获得飞书知识空间权限；必须继续执行独立的成员权限验证。
2. 不执行测试上传、Wiki 发布、Drive 原件上传、成员变更、空间创建、删除、跨空间操作或权限修改，除非用户针对对应远端操作另行明确确认。
3. 管理员与普通员工都可以查询、收录和发布符合企业门禁的企业知识；普通员工保持空间成员角色，不得成为空间管理员或自行提高策略。
4. 管理员和普通员工的个人知识一律纯本地，不能进入飞书发布、Drive 上传、员工镜像或公司同步状态。
5. 任何租户、空间、目录映射、成员权限、公司策略或证据矛盾都必须失败关闭，不得猜测、公开或静默降级。

## 完成判定

只有以下条件同时满足时，才能报告插件安装完成并继续对应角色的配置：

- 插件清单和版本有效；
- 只暴露一个 `企业知识库` Skill；
- 安装内容不含被排除的运行组件；
- Windows 或 macOS 本地路径已按当前系统解析；
- Codex 项目、Obsidian Vault 与本地绑定记录是同一规范路径；
- 默认项目名为 `知识库`，绑定记录标记 `skill_scope: project-only`；
- Claudian 已安装并启用，且其 Codex 提供方指向已验证的本机 Codex CLI；
- 已读取新安装的 `SKILL.md` 并在同一对话进入首次配置；
- 尚未获得单独授权的真实飞书写入保持为零。
